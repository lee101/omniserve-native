build*/
.cache/
.local/
.venv/
__pycache__/
*.py[cod]
*.log
*.pid
*.sock
compile_commands.json
.coverage
.pytest_cache/
performance/*.callgrind.out
performance/*.massif.out
evals/

# Locally built shared object used by matte/ctypes_candidate.py. The .npy
# fixtures under matte/fixtures/ are golden reference data and ARE committed.
matte/libomatte.so

# Training data, checkpoints, and generated model artifacts never belong in
# the source repository. Publish reviewed artifacts to a dedicated model repo.
data/
training-state/
checkpoints/
artifacts/
*.nemo
*.safetensors
*.onnx
*.gguf

# Local credentials and machine-specific configuration.
.env
.env.*
!.env.example
.runpod-*.env
secrets/

# Host-local on-call automation: agent state, cooldown stamps, and logs are
# machine-specific. The signals it consumes (/metrics, /errors) are part of the
# gateway's documented API and are specified in the README.
monitoring/

# Matting eval harness: local venv and caches (the .npy fixtures are committed).
matte/.venv/
matte/__pycache__/
