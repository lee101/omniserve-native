[Service]
# The gateway is supervised and reloads its models on failure. Large native
# cores can take many minutes to collect and keep systemd from starting the
# replacement process; production availability is more useful than a core for
# this service because the journal retains the fatal assertion and backtrace.
LimitCORE=0
